import appConstants from "common/appConstants";

export const prefix = "user";
export const prefixPath = `${appConstants.featurePrefix}.${prefix}`;

export const SET_USER_SORT_ACTION_TYPE = `${prefix}/SET_USER_SORT`;
